[build]
builder = "NIXPACKS"

[build.config]
pythonRequirementsFile = "backend/requirements.txt"
pythonSubdirectory = "backend"

[deploy]
startCommand = "cd backend && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/health"
restartPolicyType = "ALWAYS"
